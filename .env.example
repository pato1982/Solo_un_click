# ============================================================
# VARIABLES DE ENTORNO FRONTEND — Solo a un Click (Vite)
# Copiar como .env y completar los valores reales
# NUNCA subir .env a git (ya está en .gitignore)
# ============================================================

# --- Backend API URL ---
VITE_API_URL=https://soloaunclick.cl/api

# --- Sentry (error tracking frontend) ---
# Obtener DSN en https://sentry.io — crear proyecto React/Browser
# Dejar vacío para deshabilitar; Sentry solo se inicializa si hay DSN
VITE_SENTRY_DSN=
